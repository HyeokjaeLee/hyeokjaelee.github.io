# 덕업일치 개발자

> 덕업일치 개발자의 블로그입니다.

## QA 에이전트 동시 실행 큐와 Graviton 마이그레이션

- URL: https://hyeokjaelee.github.io/concurrent-queue-hard-stop/
- Date: 2026-04-05
- Tags: journal

DAG 스케줄러를 만들고 나니 다음 문제가 눈에 보이기 시작했다.

스케줄러가 실행 가능한 케이스를 식별하면, 그걸 실제로 어떻게 실행할 거냐는 문제다.

한 번에 다 실행하면 브라우저가 몇 개든 뜰 수 있고, 서버 메모리가 터진다.

순서를 정하는 것과 실제로 실행하는 건 다른 레이어의 문제다.

## 동시 실행 큐

실행 큐를 만들었다.

스케줄러가 "이 케이스들은 지금 실행 가능하다"라고 판정하면, 큐에 넣는다.

그리고 가용한 lane 수만큼만 큐에서 꺼내서 실행한다.

lane이 3개면 동시에 3개 케이스만 실행되고, 나머지는 큐에서 대기한다.

하나가 끝나면 다음 것이 큐에서 빠져나온다.

이 구조가 단순해 보이지만 구현할 때 신경 쓸 게 많았다.

큐에 넣는 시점과 lane에서 빼는 시점이 다르니까, 중간에 상태가 바뀔 수 있다.

예를 들어 큐에 있는 동안 선행 케이스가 실패해서 이 케이스가 blocked 되어야 할 수도 있다.

그러면 큐에서 꺼내기 전에 상태를 다시 확인해야 한다.

## hard stop

실행 중인 케이스가 응답 없이 멈추는 경우가 있었다.

LLM이 응답하지 않거나, 브라우저가 페이지 로딩에서 무한 대기에 빠지거나.

이런 케이스를 방치하면 전체 실행이 끝나지 않는다.

하나가 멈추면 뒤에 있는 케이스들이 전부 대기 상태가 된다.

각 케이스에 타임아웃을 설정하고, 시간이 초과되면 강제로 종료하는 hard stop 기능을 구현했다.

케이스마다 기본 15분 타임아웃을 걸었다.

15분이 지나면 실행 에이전트를 강제 종료하고, 브라우저 세션도 정리한다.

좀비 프로세스가 남지 않게 브라우저 정리까지 같이 처리한다.

hard stop이 실행되면 해당 케이스는 failed로 처리된다.

> hard stop은 가혹해 보이지만 없으면 안 된다. 하나의 멈춘 케이스가 전체 실행을 인질로 잡기 때문이다.

## Graviton 마이그레이션

Alpha 환경을 Graviton 인스턴스(t4g.xlarge)로 마이그레이션했다.

Graviton은 ARM 기반 프로세서라서 x86 대비 가성비가 좋다.

같은 가격으로 더 많은 코어를 쓸 수 있다.

문제는 ARM64 아키텍처라서 일부 npm 패키지가 호환되지 않는다는 거다.

native addon을 쓰는 패키지들은 ARM용 바이너리가 없으면 빌드가 실패한다.

호환되는 버전으로 올리거나 대체 패키지를 찾아서 하나씩 해결했다.

특히 이미지 처리와 브라우저 자동화 쪽에서 ARM 호환성 문제가 많았다.

## qa-sheet-writer 판단 기준

qa-sheet-writer의 `skipAgentTest` 판단 기준을 정리했다.

에이전트가 자동화할 필요가 없는 케이스를 명확히 구분하는 기준이다.

예를 들어 "페이지가 열리는지 확인" 같은 단순 검증은 에이전트 없이도 할 수 있다.

이런 케이스를 에이전트가 판단해서 건너뛰도록 기준을 정의했다.

메모 작성 지침도 개선했다.

PM이 결과를 읽을 때 어떤 정보가 필요한지 기준을 잡았다.

실패한 케이스에 "왜 실패했는지", "재현 방법", "예상 동작" 세 가지가 들어가도록 지침을 정했다.

## 마무리

동시 실행 큐와 hard stop으로 실행 안정성이 크게 올라갔다.

이제 케이스가 멈춰도 전체 실행이 끝나지 않는 일이 없다.

Graviton 마이그레이션으로 인프라 비용도 줄었다.

---

## OpenCode 세션 격리와 Context-Gateway 플러그인 설계

- URL: https://hyeokjaelee.github.io/opencode-session-isolation-context-gateway/
- Date: 2026-03-21
- Tags: journal

QA 에이전트를 만든 지 사흘째다.

이틀 동안 lane과 캐싱을 정리했으니, 오늘은 실행 환경 자체를 바꿨다.

그리고 사이드 프로젝트인 Context-Gateway를 OpenCode 플러그인으로 통합하는 설계를 잡았다.

## opencode server 기반 세션 전환

기존에는 QA 에이전트가 일회성 CLI로 실행됐다.

CLI로 실행하면 실행이 끝나면 상태가 사라진다.

캐싱을 해놔도 다음 실행 때 다시 로드해야 한다.

그래서 opencode server 기반 세션 구조로 전환했다.

서버가 계속 떠 있으니까 상태가 유지된다.

캐시도, planning 결과도, fixture knowledge도 서버 메모리에 남아 있다.

### lane 간 세션 공유 금지

서버 기반으로 바꾸면서 중요한 결정을 했다.

lane 간 세션 공유를 금지했다.

이전에는 lane들이 같은 세션을 공유했다.

공유하니까 문제가 생겼다.

한 lane이 페이지를 이동하면 다른 lane의 브라우저 상태도 바뀐다.

lane이 3개면 3개가 각각 독립적인 세션을 가져야 한다.

lane마다 별도의 세션 ID를 할당해서 완전히 격리했다.

한 lane에서 일어나는 일이 다른 lane에 영향을 주지 않는다.

## Context-Gateway 플러그인 설계

Context-Gateway는 컨텍스트를 압축해서 LLM 호출 비용을 줄이는 도구다.

독립 도구로 만들까 하다가 OpenCode 플러그인으로 통합하기로 했다.

### 왜 플러그인인가

독립 도구면 사용자가 Context-Gateway를 따로 설치하고 API 키를 따로 설정해야 한다.

설치 단계가 하나 더 있으면 그만큼 쓰기 어렵다.

플러그인으로 만들면 OpenCode에 설치 한 번으로 끝난다.

별도 API 키 없이 OpenCode에 등록된 provider 설정을 재사용한다.

### 사이드카 구조

Context-Gateway 바이너리를 플러그인에 사이드카 형태로 내장했다.

플러그인이 로드되면 게이트웨이가 자동으로 시작된다.

사용자는 게이트웨이가 있다는 것조차 의식하지 않아도 된다.

TypeScript와 Bun 런타임 기반이라서 npm, pnpm, bun 패키지 매니저를 전부 지원한다.

GitHub Releases와 postinstall 스크립트로 플랫폼별 바이너리를 자동 배포한다.

macOS arm64/x64, Linux x64/arm64, Windows x64를 지원한다.

### SDK 기반 설정 조회

설정 파일을 직접 파싱하지 않고 OpenCode SDK를 통해 조회한다.

provider 목록, API 키, 컨텍스트 윈도우 크기를 SDK에서 가져온다.

이 방식으로 압축용 LLM 선택, 모델별 최적 압축 임계값 계산, 인증 처리를 한 흐름으로 통합했다.

> SDK를 쓰면 설정이 바뀌어도 플러그인 코드를 수정하지 않아도 된다. 설정 조회를 SDK에 위임하니까 자동으로 추적된다.

### JSON Schema 자동완성

기본값으로 대부분 처리되지만, 세부 조정이 필요하면 `context-gateway.json`으로 설정한다.

JSON Schema를 제공해서 IDE에서 자동완성을 지원한다.

설정 이름을 외우지 않아도 IDE가 추천해준다.

## 마무리

세션 격리로 lane 간 간섭이 사라졌다.

Context-Gateway를 플러그인으로 통합한 설계는 사용자 경험을 크게 개선할 거다.

설치 한 번이면 컨텍스트 압축이 자동으로 동작하니까.

내일은 이 설계를 실제로 구현하기 시작할 예정이다.

---

## QA 에이전트 세션 구조 전환과 영구 저장소 분리

- URL: https://hyeokjaelee.github.io/qa-session-and-storage-restructure/
- Date: 2026-03-20
- Tags: journal

Slack 결과 링크가 불일치하는 문제가 있었다.

같은 테스트 결과인데 링크를 타고 들어가면 엉뚱한 페이지가 나오거나, 스레드 제목이 테스트 대상 시트 이름과 맞지 않았다.

그리고 QA 에이전트가 CLI 기반으로 실행되다 보니 세션 상태가 매 실행마다 초기화되는 것도 문제였다.

오늘은 이 두 가지를 잡았다.

## Slack 스레드와 링크 일관성

테스트 결과 페이지 링크가 실행마다 다른 형식으로 생성되고 있었다.

링크 생성 로직을 하나로 통일했다.

그리고 Slack 스레드 제목에 시트 이름을 노출시켰다.

기존에는 "QA 실행 결과" 같은 제목이 달려서 여러 실행 결과를 구분하기 어려웠다.

이제는 "QA 실행 결과 — 회원가입 플로우 시트"처럼 어떤 시트를 검증했는지 스레드 제목만 봐도 안다.

Slack에서 과거 실행 결과를 찾을 때 검색으로 바로 걸린다.

## opencode server session 전환

그 전까지 QA 에이전트는 CLI를 일회성으로 실행하는 구조였다.

실행할 때마다 새 프로세스를 띄우고 끝나면 프로세스가 죽으면서 세션 상태가 사라진다.

이게 문제가 된 건 planning과 실행을 분리하려고 할 때였다.

planning 결과를 실행 단계로 넘기려면 세션이 살아 있어야 하는데, CLI가 죽어버리니까 상태를 전달할 방법이 없었다.

그래서 opencode server 기반 세션 구조로 전환했다.

planning 전용 세션 1개가 상시 떠 있고, 실행 lane마다 별도 세션이 할당된다.

planning 세션이 실행 계획을 수립하면, 각 lane 세션이 그 계획을 받아서 병렬로 실행한다.

세션이 살아 있으니까 실행 중에 planning 세션에 추가 질의를 하거나, 실행 결과를 다시 planning에 피드백하는 것도 가능해진다.

> CLI 기반에서 서버 기반으로 바꾸니까 디버깅도 편해졌다. 세션이 살아 있으니까 실행 중에 상태를 inspect할 수 있다.

## 영구 저장소 분리

fixture knowledge, 에이전트 스킬, planning 캐시가 전부 코드 저장소 안에 있었다.

이게 문제인 이유는 두 가지다.

첫째, 코드 저장소는 버전 관리 대상이라 fixture나 캐시 같은 런타임 데이터가 들어가면 diff가 지저분해진다.

둘째, 여러 에이전트가 같은 데이터에 접근해야 하는데 코드 저장소 안에 있으면 경로가 에이전트별로 달라진다.

이 세 가지(fixture knowledge, 에이전트 스킬, planning 캐시)를 코드 저장소 외부의 영구 저장소로 통합했다.

이제 모든 에이전트가 같은 경로에서 fixture를 읽고 같은 경로에 스킬을 쓴다.

데이터 일관성이 잡혔다.

코드 저장소 diff도 깔끔해졌다.

## 마무리

서버 기반 세션 전환은 앞으로 멀티 에이전트 구조로 넘어가기 위한 기반 작업이었다.

세션이 분리되어 있으니까 각 에이전트가 독립적으로 동작하면서도 필요할 때 서로 통신할 수 있다.

영구 저장소 분리도 유지보수 측면에서 이득이다.

런타임 데이터와 코드가 섞이지 않으니까 각각 관리하기 편하다.

---

## QA 에이전트 캐싱 정책과 모델 preset 분리

- URL: https://hyeokjaelee.github.io/qa-caching-and-model-preset/
- Date: 2026-03-19
- Tags: journal

어제 lane 동적 선택과 Slack 정리를 했으니, 오늘은 실행 비용을 줄이는 작업을 했다.

QA 에이전트는 실행할 때마다 LLM을 호출한다.

케이스가 30개면 planning, execution, inspection 단계에서 수십 번의 LLM 호출이 발생한다.

비용이 문제였다.

## Slack 출력 개선

먼저 Slack 결과 메시지를 더 읽기 쉽게 바꿨다.

어제 첨부 정책을 단순화했으니까 오늘은 내용 자체를 다듬었다.

케이스 결과를 간결하게 줄였다.

이전에는 통과한 케이스도 전체 실행 로그를 메시지에 담았다.

통과한 케이스는 "통과" 한 줄이면 충분하다. 실패한 케이스만 상세를 보여준다.

스크린샷에 페이지 URL을 포함시켰다.

실패한 케이스의 스크린샷만 봐서는 어느 페이지인지 알기 어려웠다.

URL이 있으면 바로 페이지로 이동해서 확인할 수 있다.

케이스 제목도 변경했다.

이전에는 케이스 번호가 제목이었다. "TC-005 통과" 이런 식.

이걸 검증 조건 우선으로 바꿨다. "로그인 실패 시 에러 메시지 표시 — 통과" 이런 식.

제목만 봐도 뭘 검증했는지 알 수 있다.

## 캐싱 정책

가장 큰 작업은 캐싱이었다.

같은 시트를 다시 실행할 때, 전체 planning을 처음부터 다시 하면 비용이 든다.

시트 단위 캐시를 만들었다.

한 번 planning한 결과를 캐시해둔다.

다음에 같은 시트를 실행하면 캐시된 planning 결과를 가져온다.

전체 planning을 생략하니까 실행 시작이 훨씬 빨라진다.

### 부분 재계획

캐시가 있다고 해서 무조건 캐시를 쓰면 안 된다.

시트가 변경되면 변경된 케이스만 다시 planning해야 한다.

전체 캐시를 버리지 않고, 변경된 케이스만 부분 재계획하도록 했다.

diff를 계산해서 변경된 부분만 새로 planning하고, 나머지는 캐시를 유지한다.

### 스킬 수명주기

성공한 케이스의 실행 지식을 스킬로 재사용하는 수명주기를 정립했다.

예를 들어 "로그인 페이지에서 아이디 입력 필드는 #username 셀렉터를 쓴다" 같은 지식이 실행 중에 발견되면, 이걸 스킬로 저장한다.

다음에 같은 페이지를 테스트할 때 이 스킬을 재사용한다.

매번 셀렉터를 새로 찾지 않아도 된다.

## preset 기능

런타임에 모델 설정을 인라인으로 관리하던 걸 provider별 preset 모듈로 분리했다.

이전에는 코드 안에 `model: "gpt-4"` 같은 설정이 직접 박혀 있었다.

모델을 바꾸려면 코드를 수정해야 했다.

provider별 preset 모듈을 만들었다.

Anthropic preset, OpenAI preset, ZAI preset 각각 모델 구성을 별도로 관리한다.

모델을 바꿀 때는 preset 파일만 수정하면 된다.

코드를 건드리지 않아도 모델 구성을 바꿀 수 있다.

> 설정은 코드와 분리하는 게 기본이다. 설정이 코드 안에 있으면 변경할 때마다 빌드를 다시 해야 한다.

## 마무리

캐싱으로 반복 실행 비용을 크게 줄였다.

같은 시트를 두 번째 실행할 때 planning 비용이 거의 0에 가깝다.

preset 분리로 모델 관리도 편해졌다.

내일은 실행 구조를 더 개선할 예정이다.

---

## QA 에이전트 lane 동적 선택과 Slack 메시지 정리

- URL: https://hyeokjaelee.github.io/qa-agent-lane-and-slack-cleanup/
- Date: 2026-03-18
- Tags: journal

QA 에이전트를 만들기 시작한 지 이틀째다.

어제는 기본 파이프라인을 세우고 lane이라는 개념을 도입했다.

lane은 병렬 실행 단위다. 한 번에 여러 테스트 케이스를 돌릴 수 있는 채널이다.

오늘은 이 lane을 어떻게 운영할지, 실행 결과를 Slack에 어떻게 보낼지 정리했다.

## lane 실행 로그 통합

lane별로 실행 로그가 세 개 파일로 나뉘어 있었다.

요약 로그, 상세 결과 로그, 에러 리포트가 각각 별도 파일이었다.

누가 어떤 로그를 봐야 하는지 헷갈렸다.

세 개를 하나로 합쳤다.

하나의 로그 파일에 lane 번호, 실행 시간, 결과, 에러가 시간순으로 쌓인다.

로그를 하나씩 뒤지지 않아도 전체 흐름이 한눈에 보인다.

## lane 수 동적 선택

처음에는 lane을 3개로 고정했다.

그런데 케이스가 5개일 때와 30개일 때 필요한 lane 수가 다르다.

5개 케이스에 lane 3개면 충분하지만, 30개 케이스에 lane 3개면 실행 시간이 너무 길어진다.

반대로 케이스가 2개인데 lane 3개면 하나는 놀고 있다.

플래너가 케이스 특성에 따라 lane 수를 동적으로 선택하도록 개선했다.

플래너는 전체 케이스 수와 의존성 구조를 보고 1부터 5까지 lane 수를 결정한다.

케이스가 적으면 lane을 줄이고, 많고 의존성이 낮으면 lane을 늘린다.

## Slack 메시지 정책

Slack `/qa` 명령어로 QA를 실행하면 결과가 Slack에 온다.

기존에는 결과 메시지에 첨부 파일이 너무 많았다.

요약, 상세 결과, 스크린샷, 에러 리포트를 전부 첨부하니까 메시지가 길어져서 읽기 어려웠다.

첨부 정책을 단순화했다.

요약 메시지에 가장 중요한 정보만 담는다. 통과 수, 실패 수, 실행 시간.

상세 결과는 스레드에 넣었다. 요약 메시지에 답글로 상세 결과를 붙인다.

핵심은 "메시지를 열었을 때 바로 상태를 파악할 수 있어야 한다"는 거다.

> Slack 메시지는 가독성이 생명이다. 첨부가 많으면 사람들이 안 읽는다. 핵심만 보여주고 디테일은 접어두는 게 낫다.

## 사이드 프로젝트 DEV 검증

괴담 소설 사이드 프로젝트에서 DEV 환경 스키마로 에피소드 생성이 정상 동작하는지 검증했다.

DEV 스키마가 프로덕션과 구조가 달라서 에피소드 생성 로직을 DEV에 맞게 조정했다.

정상 동작을 확인했다.

## 옵시디언 노트 규칙

옵시디언 Daily 노트 작성 규칙을 정리했다.

매일 어떤 구조로 노트를 쓸지, 어떤 태그를 쓸지 규칙을 만들었다.

노트가 쌓이면 나중에 회고나 블로그를 쓸 때 유용하다.

## Slack 봇 Socket Mode

Slack 봇의 Socket Mode 연결 절차를 문서화했다.

Socket Mode는 Slack 앱이 HTTP 엔드포인트 없이 WebSocket으로 이벤트를 받는 방식이다.

방화벽 뒤에서도 동작하니까 개발 환경에서 쓰기 편하다.

하지만 연결 절차가 직관적이지 않아서 문서화해뒀다.

## 마무리

lane 수를 동적으로 선택하니까 케이스 수에 관계없이 적절한 실행 시간이 나온다.

Slack 메시지를 단순화하니까 결과를 빠르게 파악할 수 있다.

내일은 캐싱 정책을 손볼 예정이다.

---
