name: CD

on:
  push:
    branches:
      - main

permissions:
  contents: write

env:
  BUN_VERSION: "1.3.9"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build
        env:
          PUBLIC_GH_LIKE_CLIENT_ID: ${{ secrets.PUBLIC_GH_LIKE_CLIENT_ID }}
          PUBLIC_GH_LIKE_PROXY_URL: ${{ secrets.PUBLIC_GH_LIKE_PROXY_URL }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
