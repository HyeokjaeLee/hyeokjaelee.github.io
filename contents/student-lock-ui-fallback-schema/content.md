---
title: '학생 화면 잠금 UI와 auto-fallback 스키마 통합'
description: 'Phase 3 학생 화면 잠금 파트 UI 구현. 완료율 표시, 잠금 라벨, 콘텐츠 디밍, 아코디언 접힘. auto-fallback 설정 스키마 통합 리팩토링, v0.4.5 배포.'
date: '2026-05-07'
tags: [journal]
titleImage: '@shared/assets/dev-diary.png'
---

업무 프로젝트 Phase 3 UI 구현과 auto-fallback 리팩토링을 병행한 날이었다.

## 학생 화면 잠금 UI

Phase 3 학생 화면 잠금 파트 UI를 구현했다.

순차수강 모드에서는 이전 강의를 완료해야 다음 강의가 잠금 해제된다.

잠금 상태를 시각적으로 표현하는 게 핵심이다.

### 잠금 표현 방식

완료율을 표시했다.

학생이 전체 강의 중 몇 개를 완료했는지 프로그레스바로 보여준다.

잠긴 강의에는 잠금 라벨과 부제목이 표시된다.

"이전 강의를 완료하면 학습할 수 있습니다" 같은 안내 문구.

잠긴 강의의 콘텐츠는 흐릿하게 보인다.

CSS 디밍 처리로 잠금 상태를 직관적으로 전달한다.

아코디언도 잠긴 상태에서는 접혀 있어서 학생이 실수로 열 수 없다.

### Oracle 검증

Oracle 검증으로 3가지 문제를 발견해서 수정했다.

Oracle 검증은 실제 환경에서 기대한 대로 동작하는지 확인하는 검증이다.

잠금 상태에서 프로그레스바가 100%로 표시되는 버그, 디밍 처리가 안 되는 엣지 케이스, 아코디언 접힘 상태가 유지되지 않는 버그를 수정했다.

## auto-fallback 스키마 통합

auto-fallback 설정 스키마를 통합 리팩토링했다.

활성화 상태의 타입을 명확히 하고, 기본값을 비활성으로 통일했다.

이전에는 활성화 상태가 문자열과 불린이 혼용되어 있어서 타입이 불안정했다.

컨텍스트 예약 토큰을 제거하고 임계점을 100%로 최적화했다.

v0.4.5를 배포했다.

### registered 분류 버그

registered 분류 버그를 수정했다.

모델이 등록 상태를 잘못 분류해서 활성 모델이 비활성로 처리되는 문제였다.

auto-update 토스트에 재시작 안내를 추가했다.

업데이트 후 재시작이 필요하다는 걸 사용자에게 알려주는 기능이다.

재시작 안내 없이 업데이트만 하면 사용자가 새 기능이 안 보여서 당황한다.

## 마무리

학생 화면 잠금 UI는 디자이너의 Figma와 픽셀 단위로 맞췄다.

디밍과 아코디언 같은 디테일이 사용자 경험에 큰 영향을 미친다.

auto-fallback 스키마 통합으로 타입 안정성이 올라갔다.
